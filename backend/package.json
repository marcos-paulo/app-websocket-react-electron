{
  "name": "websocket-backend",
  "version": "1.0.0",
  "description": "WebSocket server that terminates when client disconnects",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "NODE_ENV=production node dist/server.js",
    "dev": "ts-node src/server.ts",
    "dev:watch": "nodemon --exec ts-node src/server.ts",
    "build:all": "npm run build && cd ../frontend && npm run build",
    "start:prod": "npm run build:all && NODE_ENV=production node dist/server.js"
  },
  "keywords": [
    "websocket",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "@types/ws": "^8.5.8",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}